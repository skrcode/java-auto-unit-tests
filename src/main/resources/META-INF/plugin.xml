<!-- Plugin Configuration File -->
<idea-plugin>
    <id>com.github.skrcode.javaautounittests</id>
    <name>JAIPilot – One-Click AI Agent for Java Unit Testing</name>
    <vendor email="surajkrajan95@gmail.com" url="https://skrcode.github.io">Suraj Krishnan Rajan</vendor>

    <!-- Dependencies -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <depends>JUnit</depends>

    <!-- Description -->
    <description><![CDATA[
    <p><strong>AI-Powered Unit Tests for Java — generate, run, and fix tests automatically inside IntelliJ.</strong></p>

    <em>JAIPilot – One-Click AI Agent for Java Unit Testing</em> creates complete, context-aware <strong>JUnit tests</strong> in seconds — right inside your IDE.</p>

    <ul>
      <li>🧠 <strong>Understands your code</strong> — analyzes method logic, parameters, dependencies, and edge cases to generate meaningful tests</li>
      <li>⚡ <strong>One-click generation</strong> — right-click any class or method to instantly create high-coverage, runnable JUnit tests</li>
      <li>🔁 <strong>Autonomous refinement</strong> — automatically runs tests, detects failures, and fixes them until they pass</li>
      <li>🚀 <strong>Optimized AI models</strong> — dynamically picks the best AI model per class for speed and accuracy</li>
      <li>📂 <strong>Custom test root</strong> — choose where tests go (e.g., <code>src/test/java</code>)</li>
      <li>🧪 <strong>Seamless JUnit integration</strong> — works out-of-the-box with your existing Java projects</li>
      <li>💸 <strong>Free credits on signup</strong> — get started instantly with no credit card required</li>
    </ul>

    <p>Skip the repetitive test writing. Let JAIPilot handle the heavy lifting — so you can focus on real engineering.<br/>
    Join other developers using JAIPilot to <strong>ship faster, test smarter, and build with confidence.</strong></p>

    <p><em>Works seamlessly with JUnit 4, JUnit 5, and TestNG projects in IntelliJ IDEA.</em></p>
    ]]></description>


    <!-- Actions -->
    <actions>
        <group id="AIAutoUnitTestGroup"
               popup="true"
               text="JAIPilot – One-Click AI Agent for Java Unit Testing">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
            <add-to-group group-id="ProjectViewPopupMenu" anchor="first"/>

            <action id="GenerateTestsAction"
                    class="com.github.skrcode.javaautounittests.GenerateTestAction"
                    text="Generate Tests"/>

            <action id="OpenSettingsAction"
                    class="com.github.skrcode.javaautounittests.settings.OpenAISettingsAction"
                    text="Settings"/>
        </group>
    </actions>

    <!-- Extensions -->
    <extensions defaultExtensionNs="com.intellij">
        <toolWindow
                id="JAIPilot Console"
                anchor="bottom"
                factoryClass="com.github.skrcode.javaautounittests.settings.TestGenerationToolWindowFactory"
                icon="/icons/jaipilot.svg"
                canCloseContents="false"/>

        <applicationService serviceImplementation="com.github.skrcode.javaautounittests.settings.AISettings"/>
        <projectConfigurable instance="com.github.skrcode.javaautounittests.settings.AISettingsConfigurable"/>
        <statusBarWidgetFactory id="AIStatusWidget" implementation="com.github.skrcode.javaautounittests.settings.AIStatusWidgetFactory"/>
        <notificationGroup
                id="JAIPilot - One-Click AI Agent for Java Unit Testing Feedback"
                displayType="BALLOON"
                isLogByDefault="false"/>
    </extensions>

    <resource-bundle>messages.MyBundle</resource-bundle>
</idea-plugin>
